name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 90
    runs-on: ubuntu-latest

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      # ✅ 리포트 내부에 test-results 복사 (스크린샷/비디오 포함)
      - name: Copy test-results into report folder
        if: always()
        run: |
          mkdir -p playwright-report/test-results
          if [ -d test-results ]; then cp -r test-results/* playwright-report/test-results/; fi

      - name: 테스트 결과 업로드 (선택)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 30

      - name: HTML 리포트 업로드 (선택)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      # ✅ GitHub Pages에 HTML 리포트 자동 배포
      - name: GitHub Pages로 HTML 리포트 배포
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: playwright-report
          publish_branch: gh-pages
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      # ✅ 슬랙 알림 전송
      - name: 슬랙 알림 전송
        if: always()
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🎯 *Playwright 테스트 완료*\n- 상태: `${{ job.status }}`\n- 리포트: https://beddy724.github.io/playwright-practice/"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}

  